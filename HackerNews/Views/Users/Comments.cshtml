@model HackerNews.ViewModels.Users.UserCommentsListView
@{
    ViewData["Title"] = "Comments";
}

<h2>Comments</h2>

@foreach (var item in Model.GetModels)
{
    <tr>
        @{ 
        }
        <td>@item.Id</td>
        <td>@item.Text</td>
        @*<td>
            @board.Title
        </td>*@
    </tr>
}

